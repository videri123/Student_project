apiVersion: v1
kind: Service
metadata:
  name: student-mlflow-app-service
spec:
  selector:
    app: student-mlflow-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30090
  type: NodePort